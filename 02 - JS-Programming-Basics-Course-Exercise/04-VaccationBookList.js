function vaccationBookList(pageBook, pageRead, days){
    let totalTime = pageBook / pageRead;
    let hoursRead = totalTime / days ;
    console.log(hoursRead);

}

vaccationBookList (212,20,2);
vaccationBookList (432,15,4);